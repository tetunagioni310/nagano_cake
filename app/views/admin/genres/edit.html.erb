<h2>ジャンル編集</h2>

<%= form_with model: @genre, url: admin_genre_path, method: :patch do |f| %>
  <div class="add-genre">
    <%= f.label :name, "ジャンル名" %>
    <%= f.text_field :name, placeholder: "ジャンル名", class: "form-size" %>
    <%= f.submit "変更を保存", class: "btn2" %>
  </div>
<% end %>